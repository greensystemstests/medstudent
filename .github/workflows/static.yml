# Build the Vite app, then deploy the built output (dist/) to GitHub Pages.
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["claude/intelligent-curie-jsa86h", "codex/**"]
    # NOTE: add "main" here (or switch to it) once this branch is merged
    # into main and main becomes the branch you deploy from.

  # Allows you to run this workflow manually from the Actions tab
  pull_request:
    branches: ["claude/intelligent-curie-jsa86h"]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages-${{ github.ref }}"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    # Real Postgres for the account/document API tests (server/accounts.test.js).
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: studybg
          POSTGRES_PASSWORD: studybg
          POSTGRES_DB: studybg_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U studybg"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Type check
        run: npm run lint
      - name: Route rendering smoke check
        run: npm run test:render
      - name: API tests (PostgreSQL 16, disposable studybg_test database)
        # The concurrency tests (single-use sign-in codes, duplicate webhooks -> one invoice)
        # need independent PostgreSQL sessions, so this job must run them, never skip them.
        run: |
          npm test 2>&1 | tee api-tests.log
          grep -q '^# skipped 0$' api-tests.log || { echo "::error::Some API tests were skipped; the PostgreSQL concurrency tests must run."; exit 1; }
          grep -q 'ok .* - concurrent code verification consumes a code exactly once' api-tests.log
          grep -q 'ok .* - concurrent duplicate webhooks allocate only one invoice' api-tests.log
        env:
          TEST_DATABASE_URL: postgres://studybg:studybg@localhost:5432/studybg_test
          PGLITE_TEST: "false"
      - name: Build
        run: npm run build
        env:
          # Public URL of the payments API (Render). Override with a repository variable if it moves.
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || 'https://studybg-api.onrender.com' }}
      - name: Setup Pages
        if: github.ref == 'refs/heads/claude/intelligent-curie-jsa86h'
        uses: actions/configure-pages@v5
      - name: Upload artifact
        if: github.ref == 'refs/heads/claude/intelligent-curie-jsa86h'
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload only the built output, not the whole repo/source
          path: 'dist'

  deploy:
    if: github.ref == 'refs/heads/claude/intelligent-curie-jsa86h'
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
